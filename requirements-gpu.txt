# Use the PyTorch / torchvision pair that is already installed.
# Colab: keep its CUDA pair. CPU verification used torch 2.14 / torchvision 0.22.
# Install PyTorch and torchvision separately in a fresh environment.
torchmetrics==1.8.2
torch-fidelity==0.3.0
numpy>=1.26,<3
matplotlib>=3.7,<4
